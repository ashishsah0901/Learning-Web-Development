<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- this is the way to add the paramenters received from the template  -->
    <title>#{title}</title>
    <!-- this is how we include our CSS file -->
    style 
        include ../static/style.css
</head>

<body>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/">About</a></li>
            <li><a href="/">Contact Us</a></li>
        </ul>
    </nav>
    <div class="container">
        <h1>
            This is a gym membership form.
        </h1>
        <!-- <p>
            | is compulasary to use if we're using a plain text without suroundering it with any tag in the same line
            | #{content}
        </p> -->
        <!-- by specifying method= post we can hide the arguments to be displayed in the url  -->
        <form action="/" id="contact" method="POST">
            <input type="text" id="name" name="name" placeholder="Enter your name...">
            <input type="text" id="age" name="age" placeholder="Enter your age...">
            <input type="text" id="gender" name="gender" placeholder="Enter your gender...">
            <textarea name="address" id="address" cols="30" rows="10" placeholder="Enter your Address..."></textarea>
            <textarea name="more" id="more" cols="30" rows="10" placeholder="More about you..."></textarea>
            <button id="btn">Submit</button>
        </form>
    </div>
</body>

</html>